# 配布物（組込版Python）に同梱する、Pythonの依存パッケージ（#168）。
#
# Viewerが使うのは、HTML出力（render_html）が中心である。`typst`（約59 MB）は、Typstを通す処理（Graphviz・Pikchr・
# CeTZなどの図。#237）のために、同梱する（#263）。Mermaid用の`playwright`（約106 MB。ブラウザを起動して描画する）は、
# 同梱しない（Mermaidは、ElectronのChromiumで描画する。#207）。
# 版は、リポジトリ直下のpyproject.tomlの`dependencies`と、そろえる（tests/test_viewer_dist_requirements.pyが確認する）。
markdown-it-py==4.2.0
mdit-py-plugins==0.6.1
PyYAML==6.0.3
platformdirs==4.11.9
typst==0.15.0
